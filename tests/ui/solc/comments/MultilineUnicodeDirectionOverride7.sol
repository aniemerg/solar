contract C {
    function f() public pure { //~ ERROR: expected contract item (function, variable, struct, or modifier definition), found `<eof>`
        /* LRO‭ LRE‪ RLE ‫  PDF‬ RLO‮ PDF ‬ PDF‬ //~ ERROR: unterminated block comment
    }
}
