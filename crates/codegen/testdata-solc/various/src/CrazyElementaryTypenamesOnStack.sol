// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract CrazyElementaryTypenamesOnStack {
    function f() public returns (uint256 r) {
        uint256;
        uint256;
        uint256;
        uint256;
        int256 x = -7;
        return uint256(x);
    }
}
